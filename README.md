# Expense Tracker

A responsive Expense Tracker built with HTML and CSS to practice frontend development and clean code. The `main` branch contains the refactored version, while the `messy_code` branch keeps the original AI-generated version for comparison.

# Expense Tracker — Progress

**Done:** Signup/Login/Logout (localStorage "users" + "LoggedIn"), password show/hide toggle, Add Expense form saving to "expenses" key, History page showing all expenses in a table, category filter buttons (All/Food/Transport/Bills/Shopping), Delete expense by id, Home page recent expenses (last 2), and dashboard cards (Total Expense, This Month, Total Entries).

**Remaining:** Per-user expenses — tag each expense with `userEmail` when saving, and filter by current logged-in user's email when displaying (history, home, recent, totals) so users don't see each other's data. Also worth doing: clear old test data (saved without `userEmail`) before testing this.

**Tech Stack:** HTML5 • CSS3 • Git • GitHub